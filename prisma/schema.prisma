generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./db"
}

model acomodacao {
  id          String @id @default(cuid())
  name        String
  type        String
  country     String
  value       String
  image       String
  description String
}
